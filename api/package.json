{
  "name": "api",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "gen-schema": "pnpm --filter quiz-api-spec gen-openapi-schema"
  },
  "dependencies": {
    "hono": "^4.8.10"
  },
  "devDependencies": {
    "wrangler": "^4.27.0"
  }
}
