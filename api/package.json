{
  "name": "api",
  "type": "module",
  "scripts": {
    "build": "run-s build:cf-typegen build:gen-schema typecheck",
    "build:cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "build:gen-schema": "pnpm --filter quiz-api-spec build && pnpm format",
    "deploy": "wrangler deploy --minify",
    "dev": "wrangler dev",
    "format": "biome check --write .",
    "lint": "biome check .",
    "test": "run-p test:*",
    "test:api": "jest --config=jest.config.cjs",
    "test:api:watch": "pnpm test:api --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.9.0",
    "neverthrow": "^8.2.0",
    "qs": "^6.14.0",
    "zod": "^4.0.16"
  },
  "devDependencies": {
    "@biomejs/biome": "2.1.3",
    "@tsconfig/strictest": "^2.0.5",
    "@types/jest": "^29.5.14",
    "@types/node": "^20.19.10",
    "@types/qs": "^6.14.0",
    "jest": "^29.7.0",
    "npm-run-all2": "^8.0.4",
    "pactum": "^3.8.0",
    "pactum-matchers": "^1.1.7",
    "ts-jest": "^29.4.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.28.1"
  }
}
