{
  "name": "api",
  "type": "module",
  "scripts": {
    "build": "run-s build:cf-typegen build:gen-schema typecheck",
    "build:cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "build:gen-schema": "pnpm --filter quiz-api-spec build && pnpm format",
    "db:mig:apply": "wrangler d1 migrations apply quiz-db --local",
    "db:mig:create": "wrangler d1 migrations create quiz-db init",
    "db:reset": "rm -rf .wrangler/state && pnpm db:mig:apply && pnpm db:seed",
    "db:seed": "wrangler d1 execute quiz-db --local --file migrations/dev/seed.sql",
    "deploy:dev": "wrangler deploy --env dev --log-level debug",
    "deploy:test": "wrangler deploy --env test --log-level info",
    "deploy:prod": "wrangler deploy --minify --log-level info",
    "dev": "pnpm dev:d1",
    "dev:d1": "wrangler dev --env dev --log-level debug",
    "dev:mock": "wrangler dev --env dev-mock --log-level debug",
    "logs": "wrangler tail",
    "logs:dev": "wrangler tail --env dev",
    "logs:mock": "wrangler tail --env dev-mock",
    "logs:prod": "wrangler tail",
    "format": "biome check --write .",
    "lint": "biome check .",
    "test": "run-p test:*",
    "test:bdd": "vitest run --coverage --config=vitest.bdd.config.ts",
    "test:bdd:watch": "vitest --config=vitest.bdd.config.ts --ui --open --watch --coverage",
    "test:tdd": "vitest run --coverage",
    "test:tdd:watch": "vitest --ui --open --watch --coverage",
    "typecheck": "tsc --noEmit",
    "mutation-test": "stryker run"
  },
  "dependencies": {
    "hono": "^4.9.0",
    "neverthrow": "^8.2.0",
    "qs": "^6.14.0",
    "zod": "^4.0.17"
  },
  "devDependencies": {
    "@biomejs/biome": "2.1.4",
    "@stryker-mutator/core": "^9.0.1",
    "@stryker-mutator/typescript-checker": "^9.0.1",
    "@stryker-mutator/vitest-runner": "^9.0.1",
    "@tsconfig/strictest": "^2.0.5",
    "@types/node": "^20.19.10",
    "@types/qs": "^6.14.0",
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "diff": "^8.0.2",
    "jsdom": "^26.0.0",
    "npm-run-all2": "^8.0.4",
    "pactum": "^3.8.0",
    "pactum-matchers": "^1.1.7",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.28.1"
  }
}
