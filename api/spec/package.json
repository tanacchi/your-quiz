{
  "name": "quiz-api-spec",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "gen-openapi-schema": "tsp compile .",
    "gen-database-schema": "dbml2sql database.dbml --dialect sqlite -o schema.raw.sql && printf 'PRAGMA foreign_keys = ON;\n\n' > schema.sql && cat schema.raw.sql >> schema.sql && rm schema.raw.sql"
  },
  "dependencies": {
    "@typespec/compiler": "1.2.1",
    "@typespec/http": "1.2.1",
    "@typespec/openapi": "1.2.1",
    "@typespec/openapi3": "1.2.1",
    "@typespec/rest": "0.72.1"
  },
  "devDependencies": {
    "@dbml/cli": "^3.13.9"
  }
}
